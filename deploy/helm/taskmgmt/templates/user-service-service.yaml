apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  type: {{ .Values.userService.service.type }}
  selector:
    app: user-service
  ports:
    - name: http
      port: {{ .Values.userService.service.port }}
      targetPort: http
      protocol: TCP